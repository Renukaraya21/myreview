# Setup guide

## Requirements

- Node.js 16+
- npm 7+

## Install

1. `npm install`
2. Copy `.env.example` to `.env`
3. (Optional) Add Firebase config in `.env`
4. `npm run dev`

## Mock auth

When `VITE_USE_MOCK_AUTH=true` (default), the app uses localStorage and demo users.
